import React from "react";

import MenuOptions from "../MenuOptions";

const File = () => {
  const options = [];

  return (
    <MenuOptions 
      options={options}
    />
  );
};

export default File;