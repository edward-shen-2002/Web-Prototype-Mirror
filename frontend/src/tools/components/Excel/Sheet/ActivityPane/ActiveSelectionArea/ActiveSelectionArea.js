import React from "react";

import "./ActiveSelectionArea.scss";

const ActiveSelectionArea = ({ activeSelectionAreaStyle }) => (
  <div 
    className="activeSelectionArea" 
    style={activeSelectionAreaStyle}
  />
);

export default ActiveSelectionArea;