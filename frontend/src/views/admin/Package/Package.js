import React from "react";

import Paper from "@material-ui/core/Paper";

const Package = () => {

  /**
   * Hospital cases:
   * Interface would be for LHINs and admins. LHINs can only send packages to organizations they belong to.
   */
  
  return (
    <Paper>
      
    </Paper>
  );
};

export default Package;