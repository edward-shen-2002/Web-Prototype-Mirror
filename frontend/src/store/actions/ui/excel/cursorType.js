import { UPDATE_CURSOR_TYPE, RESET_CURSOR_TYPE } from "actionCreators";

export const updateCursorType = () => ({ type: UPDATE_CURSOR_TYPE });
export const resetCursorType = () => ({ type: RESET_CURSOR_TYPE });
