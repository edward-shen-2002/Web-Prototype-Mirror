import { ENABLE_ROW_RESIZE_MODE, DISABLE_ROW_RESIZE_MODE } from "actionCreators";

export const enableRowResizeMode = () => ({ type: ENABLE_ROW_RESIZE_MODE });
export const disableRowResizeMode = () => ({ type: DISABLE_ROW_RESIZE_MODE });
